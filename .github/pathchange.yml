name: Trigger on Path Changes

on:
  push:
    paths:
      - 'src/**/*.txt'     # Trigger on any JavaScript file in 'src/'
      
jobs:
  echo-hello-world:  
    runs-on: ubuntu-latest  # This specifies the runner to use

    steps:
      - name: Echo hello-world
        run: echo "hello-world"
